// Configuration for Supabase cloud services
// Replace the values below with your project credentials, or leave them as placeholders
// to enter them dynamically via the application UI on startup.

export const SUPABASE_CONFIG = {
  url: 'YOUR_SUPABASE_URL',
  anonKey: 'YOUR_SUPABASE_ANON_KEY'
};
